package postgres

import "testing"

func TestConnection(t *testing.T) {
	// Test that we can connect to PG
	// If we error out, ensure a pg container is running
	// Test writing data
	// Test reading data
	// Test trying to read data that doesn't exit
}
